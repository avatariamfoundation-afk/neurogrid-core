# Replace with a real opBNB testnet RPC (do not commit secrets)
RPC_URL=https://opbnb-testnet.example
# Deployer private key (test account) - NEVER commit this to GitHub
PRIVATE_KEY=0xyour_test_private_key_here
