{
  "name": "neurogrid-core",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run core/scripts/deploy.js --network opbnbTestnet"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^3.0.0",
    "@openzeppelin/contracts": "^4.9.0",
    "ethers": "^6.6.0",
    "hardhat": "^2.16.0",
    "dotenv": "^16.0.0",
    "chai": "^4.3.7"
  }
}
