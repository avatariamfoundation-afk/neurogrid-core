{
  "name": "neurogrid-core",
  "version": "1.0.0",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network testnet"
  },
  "dependencies": {
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "dotenv": "^16.0.0",
    "ethers": "^5.7.0",
    "huggingface": "^1.0.0"  // Added for AI API calls
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "libp2p": "^0.40.0"  // Added for P2P networking basics
  }
}
