{
  "name": "neurogrid-core",
  "version": "1.0.0",
  "description": "NeuroGrid Core smart contract system",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "false17.2.3",
    "hardhat": "^2.28.0"
  }
}
